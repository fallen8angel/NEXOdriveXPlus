name: Guarded Carrot sync 20260812

on:
  push:
    paths:
      - '.github/carrot-sync-trigger-guarded-20260812'

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout NEXO
        uses: actions/checkout@v4
        with:
          ref: NEXO
          fetch-depth: 0
      - name: Preflight, merge and validate
        run: bash .github/scripts/sync_carrot_guarded_20260812.sh
