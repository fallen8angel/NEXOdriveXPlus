name: Guarded Carrot sync 20260812b

on:
  push:
    branches: [NEXO]
    paths:
      - '.github/carrot-sync-trigger-guarded-20260812b'

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout NEXO
        uses: actions/checkout@v4
        with:
          ref: NEXO
          fetch-depth: 0
      - name: Preflight, merge and validate
        run: bash .github/scripts/sync_carrot_guarded_20260812b.sh
